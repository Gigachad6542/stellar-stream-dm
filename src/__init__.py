"""Stellar stream dark matter subhalo detection pipeline.

Subpackages:
    data        — Gaia query, stream processing, PyG datasets
    simulation  — Stream generation, subhalo impulse, baryonic perturbations, noise
    models      — GNN encoder, baseline CNN, transformer, utilities
    inference   — SBI pipeline, posteriors, calibration
    analysis    — Gap catalog, model comparison, visualization
"""
