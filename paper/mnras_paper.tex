% MNRAS manuscript --- generated from paper/manuscript.md. Current-state pipeline
% (streamdf/streamgapdf). Build on Overleaf ("Monthly Notices of the RAS" template)
% or locally: latexmk -pdf mnras_paper.tex  (needs mnras.cls + mnras.bst + references.bib)
\documentclass[fleqn,usenatbib]{mnras}

\usepackage{newtxtext,newtxmath}
\usepackage[T1]{fontenc}
\usepackage{graphicx}
\usepackage{amsmath}
\usepackage{booktabs}

% Synchronized with the 2026-06-03 full report at a claims level. Regenerate from
% paper/manuscript.md before any formal submission.

\newcommand{\msun}{\ensuremath{\mathrm{M}_\odot}}
\newcommand{\kms}{\ensuremath{\mathrm{km\,s^{-1}}}}
\newcommand{\code}[1]{\texttt{#1}}

% Dual-register accessibility boxes (dependency-free).
\newenvironment{plainterms}%
  {\par\medskip\begin{quotation}\noindent\small\textbf{In plain terms.}\itshape\ }%
  {\end{quotation}\medskip}

\title[DM subhalo impacts in stellar streams]{Detecting dark-matter subhalo impacts
in Milky Way stellar streams: a validated graph-neural-network pipeline and the
limits of single-stream inference}

\author[D. W. et al.]{D.~W.$^{1}$\thanks{E-mail: placeholder@institution.edu}
\\
$^{1}$Affiliation placeholder}

\date{Draft \today}
\pubyear{2026}

\begin{document}
\label{firstpage}
\maketitle

\begin{abstract}
Dark-matter subhalos below the threshold of galaxy formation ($\lesssim10^8\,\msun$)
are a defining prediction of cold dark matter (CDM) and a discriminator against warm
(WDM), fuzzy (FDM), and self-interacting (SIDM) alternatives. Their flybys imprint
localized density gaps on thin Milky Way stellar streams. We present an end-to-end,
reproducible pipeline for detecting and interpreting these gaps: a stream simulator
built on galpy's validated action-angle distribution functions (\code{streamdf},
\code{streamgapdf}; \citealt{Bovy2014streamdf}; \citealt{SandersBovyErkal2016}), gated
by a pre-flight validation harness; a graph-neural-network (GNN) detector; and a
characterization and forward-model layer. The GINEConv detector reaches \textbf{test
AUC $0.982$} with a zero false-positive operating point and, on the real STREAMFINDER
GD-1 catalog, is \emph{in-distribution} (maximum input deviation $3.1\sigma$) and,
jointly with a model-free gap finder, recovers the known $\phi_1\approx50^\circ$ gap.
We map the detector's \textbf{completeness} across impact type --- rising with subhalo
mass ($0.48\rightarrow0.72$ over $10^{7.5}$--$10^{8.5}\,\msun$) and falling with
impact age ($0.75\rightarrow0.39$ from $0.3$ to $1.4$~Gyr) --- and show that
\textbf{single-gap characterization is degeneracy-limited}: a dedicated regression
head does not recover subhalo mass ($R^2<0$) and recovers time-since-impact only
weakly ($R^2\approx0.2$). Distinguishing DM models is consequently a \textbf{population}
measurement; an abundance+mass Asimov forecast shows favorable WDM/FDM alternatives can
separate from CDM with about two clean detections, but the CDM detectable-impact rate is
only ${\approx}0.026$ per GD-1-like stream in the floor-normalized baseline, so collecting
them requires ${\approx}300$--365 clean streams; without that low-rate floor the forecast
rises to ${\approx}1160$--1370 streams. SIDM instead requires gap-shape information. We also report a corrected
\emph{timeline forward model}: it recovers a planted impact at rank 0/36, but the
seven-stream real-data combination is null after look-elsewhere correction (Stouffer
$Z=1.40$, Fisher $p=0.28$). These timeline/multistream numbers use the corrected
particle-spray/impulse forward-model backend, not the \code{streamdf}/\code{streamgapdf}
detector-training backend, and are presented with that systematic caveat. A pre-declared
matched-\code{streamgapdf} screen further shows that a single stream gap does not identify
the perturber's internal density profile (recovered-family accuracy $42$--$58\%$, below the
$70\%$ gate, even with idealized data), so \emph{per-system} DM-model typing is not
warranted on present data and is reported only as a population forecast and a
simulation-level capability.
\end{abstract}

\begin{keywords}
dark matter -- Galaxy: structure -- methods: statistical -- methods: numerical
\end{keywords}

\section*{Plain-language summary}
The Milky Way is thought to be swarming with thousands of invisible clumps of dark
matter. The lightest ones contain no stars at all, so the only way to find them is by
the gravitational ``wake'' they leave when they pass through something visible. Thin
streams of stars --- the shredded remains of old star clusters --- are the most
sensitive detectors we have: a passing dark clump carves a small \emph{gap} in the
otherwise smooth ribbon of stars. We built an automated system to find these gaps and
infer what made them. It learns from large libraries of simulated streams, generated
with community-standard, peer-reviewed code, and then reads real streams. On simulated
data it is highly accurate (it correctly flags impacted versus smooth streams about
98\% of the time) and never raises a false alarm on a smooth stream; on the real GD-1
stream it recognizes the known gap and treats the data as familiar rather than
anomalous. The honest scientific bottom line: we can reliably \emph{detect} the gaps
made by massive, recent impacts, but several things are fundamentally hard. Inferring
the exact mass of the dark clump from a single gap is essentially impossible (many
different clumps leave look-alike gaps), though we can estimate roughly \emph{how
recently} it struck; and telling apart \emph{kinds} of dark matter requires not one
detection but a whole population of them --- as few as about two clean detections for the
most favorable theories, and many more for the rest. We also describe two further tools (a ``replay'' model that
reconstructs the impact, and a method to pool many streams) and explain what they are
designed to measure.

\section{Introduction}
The abundance of low-mass dark-matter subhalos is among the sharpest predictions
separating cold dark matter (CDM) from warm (WDM), fuzzy (FDM), and self-interacting
(SIDM) alternatives. Below the threshold of galaxy formation ($\lesssim10^8\,\msun$)
these halos hold no stars and are detectable only gravitationally. Thin, dynamically
cold stellar streams --- tidal debris of disrupted globular clusters --- are exquisite
probes: a subhalo flyby imprints a density gap and a kinematic ripple whose morphology
encodes the perturber's mass and geometry \citep{Carlberg2012,ErkalBelokurov2015}.
GD-1's gap-and-spur has been read as evidence for a dark perturber \citep{Bonaca2019},
and population analyses have begun to constrain the subhalo mass function
\citep{Banik2021}.

Turning a gap into a measurement is hard for two reasons. \emph{Degeneracy:} an impact
must be separated from baryonic perturbers, epicyclic density variations, and
selection systematics, and a single gap underdetermines the perturber's mass, impact
parameter, and epoch. \emph{Cost:} the forward model is expensive, making
likelihood-based search over many hypotheses slow. We address both with a graph neural
network (GNN) detector --- whose edge-conditioned message passing \citep{Hu2020gine}
respects the permutation symmetry of a member-star set and the locality of kinematic
perturbations --- and a constrained forward-model search we call the \emph{timeline
forward model}. Because such a detector is only as trustworthy as the simulations it
learns from, we build the training set from published, validated distribution
functions and gate every batch through a quantitative pre-flight validation harness
(\S\ref{sec:sim}) before training, then report honestly what the pipeline can and
cannot measure.

\begin{plainterms}
Dark-matter clumps too small to hold stars can only be found by their gravity. When
one flies past a stream of stars, it leaves a gap. We teach an AI to spot those gaps,
training it on carefully validated simulated streams, and then we are candid about
which questions the data can and cannot answer.
\end{plainterms}

\section{The pipeline at a glance}
The system has five independently testable stages: (1) a \emph{simulator}
(\S\ref{sec:sim}) generating smooth streams and streams with a known subhalo gap from
validated distribution functions, gated by a validation harness; (2) a \emph{detector}
(\S\ref{sec:det}), a GINEConv GNN classifying a stream as impacted or smooth,
calibrated and out-of-distribution (OOD) aware; (3) \emph{completeness and
characterization} (\S\ref{sec:compl}--\ref{sec:char}); (4) the \emph{timeline forward
model} (\S\ref{sec:fwd}); and (5) \emph{population significance} (\S\ref{sec:pop}).

\section{The stream simulator}
\label{sec:sim}
\subsection{Generation with validated distribution functions}
Streams are integrated in an \code{MWPotential2014}-class Galactic potential
\citep[galpy, C \code{dop853} integrator;][]{Bovy2015galpy}. Progenitor initial
conditions are taken directly from the galstreams \citep{Mateu2023galstreams} 6-D
track at the centre of each observed $\phi_1$ window, reproducing literature proper
motions by construction. Smooth (``no-impact'') streams are drawn from the
action-angle distribution function \code{streamdf} \citep{Bovy2014streamdf}; streams
with a single subhalo gap from \code{streamgapdf} \citep{SandersBovyErkal2016}, a
subclass of \code{streamdf} sharing the identical smooth track and differing
\emph{only} by the encoded impact. This shared-track design is deliberate: the only
systematic difference between the two training classes is the gap itself, so the
detector cannot exploit a generator artifact. A particle-spray generator,
\code{streamspraydf} \citep{Fardal2015}, is used for cross-checks.

\subsection{Subhalo encounters}
A subhalo's gravitational scale is set by its NFW scale radius $r_s=r_{200}/c$ with the
\citet{Ludlow2016} concentration--mass relation (e.g. $r_s\approx0.25$~kpc for a
$10^8\,\msun$ halo); the flyby imprints the \citet{ErkalBelokurov2015} Plummer
velocity impulse. Impact encounters span the detectable regime (mass
$10^{7.5}$--$10^{8.7}\,\msun$, impact parameter $\le0.35$~kpc), encoded analytically by
\code{streamgapdf}.

\subsection{A pre-flight validation harness}
Every generated batch is gated before use: smoothness (no-impact gap-depth
\emph{excess over the Poisson floor}, $<0.15$), length versus the galstreams track,
and impact-vs-smooth separability (AUC $>0.85$) are \emph{critical} gates; width,
kinematics, and radial-velocity checks are morphology diagnostics. The generator
passes all critical gates (excess $0.01$, length $0.75\times$, separability $0.94$),
so a no-impact stream sits at the Poisson floor (it does not mimic a perturbation)
while detectable impacts are cleanly separable. Four streams support the action-angle
model cleanly (GD-1, ATLAS, Jhelum, Orphan); Pal~5 and Fj\"orm violate the isochrone
action-angle approximation (near-circular orbits) and are excluded by an allow-list.

\begin{figure}
\includegraphics[width=\columnwidth]{figures/fig1_what_impact_looks_like.png}
\caption{What a subhalo flyby does. \emph{Top:} a smooth simulated GD-1-like stream
(\code{streamdf}). \emph{Middle:} the same stream after a $10^{8.5}\,\msun$ flyby
(\code{streamgapdf}). \emph{Bottom:} star counts along the stream; the perturbed
profile (orange) shows a localized deficit (shaded) absent from the smooth profile
(blue).}
\label{fig:impact}
\end{figure}

\begin{plainterms}
A passing dark clump pulls nearby stars slightly off course, opening a gap in the
stream over time. We simulate both kinds of stream --- untouched and gap-bearing ---
with the same trusted code, so the only difference the AI can learn is the gap itself.
An automatic check refuses any batch whose streams don't look realistic.
\end{plainterms}

\section{The detector}
\label{sec:det}
\subsection{Architecture}
Each stream's member stars form a $k$-nearest-neighbour graph ($k=8$) in normalized
$(\phi_1,\phi_2,\mu_1,\mu_2)$ space, ${\leq}1200$ stars per graph. Edge features encode
the local kinematic contrast a flyby perturbs; a GINEConv encoder \citep{Hu2020gine}
(${\sim}2.3$M parameters) with an auxiliary density-profile branch produces a graph
embedding and a binary impact/no-impact head. Training uses AdamW, cosine warm
restarts, mixed precision, and a $70/15/15$ split (seed 42) on $15\,830$ simulations
($7\,830$ impact / $8\,000$ smooth) across the four supported streams. The detection
target is \emph{detectable} impacts (realized gap-depth $>0.5$).

\subsection{Performance}
On the held-out test set the detector reaches AUC $0.982$ (Fig.~\ref{fig:roc}), best
validation accuracy $0.951$, with a zero false-positive rate at the $0.5$ operating
threshold --- it never flags a smooth stream. Temperature calibration ($T=0.54$)
leaves the ranking unchanged.

\subsection{Application to the real GD-1 catalog}
Applied to the clean external STREAMFINDER GD-1 membership catalog \citep{Ibata2021}
(811 members), the detector flags an impact (\(p_\mathrm{impact}\) $0.77$--$0.99$
depending on membership cuts), and the model-free gap finder independently locates the
known Price-Whelan--Bonaca gap at $\phi_1\approx50^\circ$ \citep{PriceWhelanBonaca2018}.
Importantly, the network's inputs are \emph{in-distribution}: the maximum standardized
feature deviation is $3.1\sigma$ with $0\%$ of features clipped, so the detector
operates within its trained regime rather than extrapolating. The realistic-noise
simulator reproduces the real catalog's feature distribution, which is what makes the
real-data prediction trustworthy.

\begin{figure}
\includegraphics[width=\columnwidth]{figures/fig2_detector_roc.png}
\caption{Detector ROC on held-out simulations (AUC $0.987$ for this checkpoint;
$0.982$ after temperature calibration); the operating point has a zero false-positive
rate.}
\label{fig:roc}
\end{figure}

\begin{plainterms}
The gap-finder is nearly perfect on simulated data and treats the real GD-1 stream as
familiar rather than anomalous, which is what lets us trust what it says about real
data.
\end{plainterms}

\section{What kinds of impacts can we detect? (completeness)}
\label{sec:compl}
Detection depends on the \emph{type} of impact. Joining the detector's verdicts on the
test set to each simulation's true parameters yields the completeness map of
Fig.~\ref{fig:compl}. Completeness rises with subhalo mass ($0.48$ at $10^{7.5}$ to
$0.72$ at $10^{8.5}\,\msun$), falls with time since impact ($0.75$ for $<0.5$~Gyr to
$0.39$ for $>1.1$~Gyr, as gaps phase-mix), and is flat in impact parameter over the
close-encounter range probed ($0$--$0.35$~kpc). Overall completeness is $0.57$ at zero
false positives, and detection is essentially a step function in realized gap
strength. The detector is, in effect, a calibrated density-gap detector: it sees
massive, recent impacts that carve deep gaps and misses weak, old impacts that perturb
only the kinematics --- pointing to proper-motion-based features as the next frontier.

\begin{figure}
\includegraphics[width=\columnwidth]{figures/fig3_completeness.png}
\caption{Detection completeness across impact type, measured on the test set. Heavier
and more recent impacts are more detectable; weak/old impacts are missed.}
\label{fig:compl}
\end{figure}

\begin{plainterms}
We reliably catch big, recent hits that gouge a clear gap. Small or ancient hits that
only nudge the stars' motions slip through, so our catch rate depends on what kind of
impact it was --- and we measured exactly how.
\end{plainterms}

\section{Characterizing the impact, and the single-gap degeneracy}
\label{sec:char}
Detecting a gap is easier than reading off what made it. Two complementary tests
quantify how much of the perturber's identity survives in a single gap. First, probing
the frozen detection embedding for the perturber's physical parameters shows it encodes
mass only weakly ($R^2\approx0.18$) and impact parameter ($R^2\approx0.02$) and epoch
($R^2\approx0.04$) essentially not at all. Second, and more tellingly, a
\emph{dedicated} multi-task regression head trained jointly with the detector
\emph{fails to recover subhalo mass at all} (test $R^2<0$, i.e. no better than the
population mean) and recovers \emph{time-since-impact only weakly} ($R^2\approx0.2$,
median error ${\approx}0.1$~dex). This is a genuine physical degeneracy: mass, impact
parameter, flyby speed, and epoch trade off in shaping a single gap's depth and width,
so one gap underdetermines them. Recency leaves a partial imprint (older gaps are wider
and shallower) and is the one property weakly constrained; the forward model of
\S\ref{sec:fwd} is the complementary route that can in principle break the degeneracy
by fitting the full joint morphology.

\begin{plainterms}
Finding the dent is easier than figuring out the exact size and speed of what caused it
--- many different impacts can leave a similar-looking dent. Some properties (roughly
how heavy, how recently) can be estimated; others are essentially unknowable from a
single gap.
\end{plainterms}

\section{Telling dark-matter models apart is a population measurement}
\label{sec:dm}
WDM, FDM, and SIDM do not change how any single impact of fixed mass looks --- they
change the subhalo population. DM-model discrimination is therefore a population
inference. Two observables carry the signal: the abundance of detectable impacts and
the mass distribution of those detections. Combining both with our measured completeness
in an Asimov likelihood-ratio forecast shows that favorable suppressed models (WDM
3~keV and FDM $10^{-22}$~eV) can separate from CDM with about two clean detections. The
sobering part is collection: the CDM detectable-impact rate is only ${\approx}0.026$ per
GD-1-like stream in the floor-normalized baseline, so obtaining those detections requires
${\approx}300$--365 clean streams. The raw no-floor normalization pushes the same stream
count forecast to ${\approx}1160$--1370 streams, making the stream count a
systematic-sensitive forecast rather than a fixed result. SIDM shares CDM's abundance and
mass function, so it needs a different handle: shallower
gap shapes from sufficiently cored, low-concentration halos.

\begin{figure}
\includegraphics[width=\columnwidth]{figures/fig4_dm_family.png}
\caption{\emph{(a)} Detected-impact mass distributions differ between DM models only
where the mass-function cutoff falls inside our sensitive band
($10^{7.5}$--$10^{8.7}\,\msun$). \emph{(b)} Number of clean detections needed from
mass-shape information alone; the full rate+mass forecast improves favorable WDM/FDM
cases but leaves SIDM to gap-shape tests.}
\label{fig:dm}
\end{figure}

\begin{plainterms}
Different dark-matter theories don't change what one impact looks like --- they change
how common small clumps are. Favorable WDM/FDM models may need only two clean detections
statistically, but finding those detections takes hundreds to over a thousand clean streams,
depending on rate normalization. SIDM needs a
different clue: shallower gaps at the same mass, if the cores are large enough.
\end{plainterms}

\begin{table*}
\caption{Dark-matter model discriminants against CDM.}
\label{tab:dm-discriminants}
\begin{tabular}{lll}
\hline
Model & Main signal & Forecast / validation status\\
\hline
CDM & Reference abundance and cuspy gaps & $\lambda_{\rm det}\approx0.026$ per stream; seven-stream null is unsurprising\\
WDM 3--4 keV & Suppressed detectable-impact abundance + detected-mass distribution & Favorable cases need ${\approx}2$--10 detections, but ${\approx}365$--770 baseline streams\\
FDM $10^{-22}$ eV & Suppressed abundance + shifted detected-mass distribution & ${\approx}2$ detections, ${\approx}311$ baseline streams; raw no-floor forecast ${\approx}1163$ streams\\
WDM 6 keV / FDM $10^{-21}$ eV & Cutoff outside or near edge of sensitive band & Effectively indistinguishable in this sample\\
SIDM & Shallower/broader gaps from sufficiently cored perturbers & No abundance handle; morphology AUC depends on core strength\\
\hline
\end{tabular}
\end{table*}

The present seven-stream sample is therefore a null-power regime rather than a decisive
model-selection regime. The same forecast predicts only $E[N_{\rm det}]=0.18$ detectable
impacts across seven GD-1-like streams under CDM, corresponding to
$P(0~{\rm detections})=0.83$ in the floor-normalized baseline and $0.95$ without the
historical low-rate floor. Favorable suppressed models also predict mostly null samples
($P_0=0.95$--$0.96$ for WDM 3 keV and FDM $10^{-22}$ eV), giving seven-stream
discrimination of only $Z=0.42$--$0.45$ relative to CDM. Thus the current real-data null
is CDM-consistent but not a constraint on WDM/FDM/SIDM; the forecasted population size is
the relevant requirement.
A 180-case sensitivity grid over rate normalization/floor, completeness, a
threshold-completeness proxy, mass band, and stream count leaves this conclusion unchanged:
even the most favorable seven-stream cases reach only $Z=0.72$ for WDM 3 keV and
$Z=0.79$ for FDM $10^{-22}$ eV. The median 3-sigma stream requirements across the grid
are 533 and 463 streams, respectively. The threshold axis is a proxy only and should be
replaced by measured threshold-specific ROC/completeness curves for any
threshold-optimization claim.

\section{The timeline forward model}
\label{sec:fwd}
To move beyond detection toward physical characterization, we designed a \emph{timeline
forward model} that, for a detected gap, (1) estimates the impact epoch, (2) rewinds
the stream to its unperturbed state, (3) re-injects a grid of encounters (mass $\times$
epoch $\times$ $\phi_1$) with the \citet{ErkalBelokurov2015} Plummer impulse and the
NFW scale radius, (4) re-evolves to the present, and (5) scores each hypothesis against
the data. It recasts detection as a constrained, physically explicit fit whose free
parameters are the impact's mass, epoch, and location --- the natural route around the
single-gap degeneracy of \S\ref{sec:char}.

\emph{Scope caveat.} The forward model runs on a corrected particle-spray generator with
an analytic impulse kick --- \emph{not} the validated \code{streamdf}/\code{streamgapdf}
distribution functions used for detector training. We therefore report its outputs as
methods/upper-limit results with a simulator-backend caveat, not as fully DF-ported
physical constraints.

\emph{Validation and real GD-1.} Injecting a known GD-1-like impact ($10^9\,\msun$,
1.5~Gyr, $\phi_1=20^\circ$) and running the recovery grid returns the truth as the
top-ranked candidate (rank 0/36). On the clean STREAMFINDER GD-1 catalog, the model-free
finder localizes the known $\phi_1\approx50^\circ$ gap. The best single-subhalo
hypothesis improves only marginally over the smooth null, and after look-elsewhere
correction the single-subhalo attribution is not significant.

Matched-control follow-up diagnostics show that the local real-GD1 fit is sensitive to
the assumed smooth-stream background and that tested profiles do not yet improve both
density/kinematics and cross-stream morphology together. A matched two-arm
\code{streamdf}/\code{streamgapdf} backend with continuous perturber scale radius is now
implemented and was put through a pre-declared injection/recovery identifiability screen
(Fig.~\ref{fig:profileval}). The screen fails its frozen gates: with the real PWB18/DESI
selection the recovered profile family is correct only $42\%$ of the time and detection
recall is $25\%$ (on idealized fully-sampled streams these rise only to $58\%$ and
$33\%$), against gates of $70\%$ and $60\%$; the recovered scale radius scatters by
$0.34$--$0.41$~dex. The lowest-mass gaps are too shallow to detect, and detected gaps
under-determine the perturber's scale radius, so selection sparsity worsens but does not
cause the failure---the dominant limit is the intrinsic gap$\rightarrow$profile
degeneracy. Per the pre-registered rule we make no subhalo-attribution, perturber-profile,
or per-system DM-model claim for real GD-1.

\begin{figure}
\includegraphics[width=\columnwidth]{figures/fig20_profile_validation.png}
\caption{Matched \code{streamgapdf} profile-identifiability screen. \emph{(a)} The two
pre-declared gates---recovered-family accuracy and detection recall---fail both with the
real GD-1 selection and on idealized fully-sampled streams (dashed red lines are the
frozen gate thresholds). \emph{(b)} Recovered vs true perturber scale-radius factor on the
clean screen: detected impacts (orange) scatter far off the $1{:}1$ line, so the
perturber's density profile is not recoverable from the gap even in the best case.}
\label{fig:profileval}
\end{figure}

\begin{plainterms}
The next step beyond ``is there a gap?'' is ``what made it?'' We built a tool that
rewinds a stream, drops in a simulated clump, fast-forwards, and checks the match. It
recovers impacts we plant ourselves and confirms the GD-1 gap is real, but it cannot yet
pin that gap on one specific clump. We also tested whether it could read a clump's
``fluffiness''---the density profile that would tell ordinary cold dark matter from warmer
or self-interacting kinds---and it cannot: even with perfect data, look-alike gaps come
from very different clumps, so we make no claim about \emph{what kind} of dark matter made
GD-1's gap.
\end{plainterms}

\section{Population significance across streams}
\label{sec:pop}
A single stream rarely yields a decisive detection, so the pipeline includes a
multi-stream combination designed to test for a \emph{population} of impacts. Per stream,
the best-scoring impact hypothesis is compared against a data-driven, gap-removed
look-elsewhere null: the null preserves the real stream's broad density envelope and
marginal kinematics, removes localized structure, searches the same full grid as the
data, and retains its best candidate. Per-stream evidence is then combined with
Stouffer's $Z$ and Fisher's method, gated by a coherence requirement.

Applied to the seven target streams, the correction is decisive: naive single-cell
significances as large as $19\sigma$ (Sylgr) and $12\sigma$ (Pal~5) collapse to about
$\pm2\sigma$ after the look-elsewhere correction. The joint result is null --- Stouffer
$Z=1.40$ ($p=0.08$) and Fisher $p=0.28$, with the coherence gate not satisfied --- so the
current real-stream result is a CDM-consistent upper limit, not a dark-matter detection.

\begin{plainterms}
A single stream rarely settles the question, so we built a way to pool many streams and
to guard against a statistical trap --- if you don't account for how many places you
looked, pure noise can masquerade as a discovery. Pooling seven streams still gives no
convincing signal; that is an upper limit, and a warning against naive many-sigma claims.
\end{plainterms}

\section{Limitations and systematics}
\label{sec:lim}
(1) Single-gap degeneracy (\S\ref{sec:char}): mass/geometry/epoch are only partially
recoverable from one gap. (2) Density-only sensitivity (\S\ref{sec:compl}): weak/old
kinematics-only perturbations are missed; a diagnostic shows the proper-motion kink is
real in noise-free simulations but below current Gaia-like precision.
(3) Action-angle model validity: clean generation is limited to eccentric streams;
multi-impact streams require \code{streampepperdf} (unavailable in this galpy), so
single-vs-multiple classification is deferred (gap counting is available model-free).
(4) Real-data volume: DM-model discrimination needs many clean streams
(\S\ref{sec:dm}). (5) \emph{Forward model on a separate generator} (\S\ref{sec:fwd}):
the timeline forward model and multi-stream significance framework run on a corrected
particle-spray + impulse backend, not the validated \code{streamdf}/\code{streamgapdf}
of \S\ref{sec:sim}; their quantitative outputs are reported with this backend caveat.
The single-encounter and impulse approximations also apply. The matched two-arm DF
backend has now been run through a pre-declared injection/recovery identifiability screen
and fails its family-recovery ($42$--$58\%$, gate $70\%$) and recall ($25$--$33\%$, gate
$60\%$) gates (Fig.~\ref{fig:profileval}), so real-data profile inference stays blocked by
a measured gap$\rightarrow$profile degeneracy.

\section{Conclusions}
We presented a complete, reproducible pipeline for dark-matter subhalo-impact searches
in stellar streams, built on validated stream distribution functions and a pre-flight
validation harness. The GINEConv detector reaches test AUC $0.982$ with a zero
false-positive operating point and is in-distribution on the real STREAMFINDER GD-1
catalog, where it and a model-free gap finder jointly recover the known
$\phi_1\approx50^\circ$ gap. With this validated pipeline we (i) mapped detection
completeness across impact type --- strong for massive, recent impacts and falling for
weak, old ones; (ii) showed that single-gap characterization is degeneracy-limited
(subhalo mass is essentially unrecoverable from one gap; only recency is weakly
constrained); and (iii) reframed DM-model discrimination as a population measurement:
favorable WDM/FDM cases can separate from CDM with about two detected impacts, but
collecting them requires roughly 300--365 clean streams in the floor-normalized baseline
and roughly 1160--1370 streams without the low-rate floor; SIDM requires gap-shape
information. The corrected timeline forward model recovers planted
impacts, but applied to seven real streams it yields no coherent multi-stream detection
after look-elsewhere correction (Stouffer $Z=1.40$, Fisher $p=0.28$). The path to a
physical measurement is concrete: more clean catalogs, breaking the
gap$\rightarrow$profile degeneracy that the matched-backend screen shows currently blocks
per-system DM typing (high-precision kinematics, multi-tracer streams), and
multi-encounter modeling.

\section*{Methods}
\textbf{Potential \& integration.} galpy \code{MWPotential2014}; C \code{dop853}
integrator; $R_0=8.0$~kpc, $V_0=220$~\kms.
\textbf{Distribution functions.} \code{streamdf} \citep{Bovy2014streamdf} for the
smooth track; \code{streamgapdf} \citep{SandersBovyErkal2016} with \code{impactb},
\code{subhalovel}, \code{timpact}, \code{impact\_angle}, $GM$, and $r_s$. The
action-angle setup uses $b=\mathrm{estimateBIsochrone}(\mathrm{pot},R/R_0,z/R_0)$
(${\approx}0.61$ for GD-1) and \code{nTrackChunks}$=5$; the impact angle shares the sign
of the modeled arm. \textbf{Subhalo physics.} NFW scale radius $r_s=r_{200}/c$ with the
\citet{Ludlow2016} concentration--mass relation and
$\rho_\mathrm{crit,0}=277.5\,h^2\,\msun\,\mathrm{kpc}^{-3}$; the
\citet{ErkalBelokurov2015} Plummer impulse
$\Delta v=-(2GM/w)\,\mathbf{b}/(|\mathbf{b}|^2+r_s^2)$. \textbf{Detector.} GINEConv
encoder, 18 node / 5 edge features, hidden 256, 6 layers, embedding 128, profile branch
(48 bins); binary head (BCE) with auxiliary mass/epoch regression; AdamW
(lr $3\times10^{-4}$, wd $10^{-4}$); temperature calibration on validation.
\textbf{Noise model.} Gaia DR3-like per-star errors \citep[][scale $0.5$--$2\times$]
{GaiaDR3}; radial velocity dropped at inference to match clean catalogs. Training on
foreground-contaminated streams collapses separability, so the operating regime is
clean membership catalogs. \textbf{Statistics.} Corrected particle-spray/impulse
forward-model backend for replay; data-driven gap-removed best-of-grid look-elsewhere
null; Stouffer/Fisher combination with a coherence gate. \textbf{Datasets.}
\code{data/simulations\_detector\_df} ($15\,830$
sims; GD-1, ATLAS, Jhelum, Orphan); detector \code{checkpoints/detector\_df\_20260602}.

\section*{Data availability}
Public repository with a conda environment specification, 330 passing unit tests and 9 skipped checks, and
per-decision changelogs. Datasets regenerate from
\code{scripts/generate\_detector\_data.py} (deterministic per seed, resumable); figures
from \code{paper/make\_figures.py}, \code{paper/make\_report\_figures.py}, and
\code{paper/make\_report\_figures2.py}; validation from
\code{scripts/validate\_generator.py}; claim provenance from
\code{paper/claims\_audit.md}.

\bibliographystyle{mnras}
\bibliography{references}

\label{lastpage}
\end{document}
